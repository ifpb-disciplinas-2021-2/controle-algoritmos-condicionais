#include <stdio.h>
int main(){
    // operadores relacionais
    // a > 10
    // a != 10 
    // a == 10 
    int a;
    printf("Digite o valor de A: ");
    scanf("%d", &a);
    // se a>10 faça ...
    if(a > 10){ //verdadeira
        printf("O valor digitado é maior que 10. \n");
    }
    
    return 0;
}